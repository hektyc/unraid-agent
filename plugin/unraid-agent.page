Menu="Utilities"
Title="Unraid MCP Server"
Icon="/plugins/unraid-agent/images/unraid-agent-48.png"
---
<?php
$plugin = "unraid-agent";
$config_file = "/boot/config/plugins/$plugin/config.cfg";

$config = [];
if (file_exists($config_file)) {
    $parsed = parse_ini_file($config_file, false, INI_SCANNER_RAW);
    if ($parsed !== false) {
        $config = $parsed;
    }
}

$cfg = function($key, $default = '') use ($config) {
    return isset($config[$key]) ? $config[$key] : $default;
};

$running = false;
$pidfile = "/boot/config/plugins/$plugin/server.pid";
if (file_exists($pidfile)) {
    $pid = trim(file_get_contents($pidfile));
    if ($pid !== '') {
        exec("kill -0 $pid 2>/dev/null", $output, $rc);
        if ($rc === 0) {
            $running = true;
        }
    }
}
?>
<div class="title"><span class="left"><i class="title fa fa-plug"></i>Unraid MCP Server</span></div>

<div class="title"><span class="left"><i class="title fa fa-plug"></i>Service Status</span></div>

<?php if ($running): ?>
<span style="font-weight:bold; color:green;">RUNNING</span>
<?php else: ?>
<span style="font-weight:bold; color:red;">STOPPED</span>
<?php endif; ?>

<form method="POST" action="/update.php" target="progressFrame" style="display:inline;" onsubmit="setTimeout(function(){location.reload();},3000);">
<input type="hidden" name="csrf_token" value="<?=$var['csrf_token']?>">
<input type="hidden" name="#command" value="/usr/local/emhttp/plugins/<?=$plugin?>/scripts/start.sh">
<input type="submit" name="#start" value="Start MCP Server">
</form>

<form method="POST" action="/update.php" target="progressFrame" style="display:inline;" onsubmit="setTimeout(function(){location.reload();},3000);">
<input type="hidden" name="csrf_token" value="<?=$var['csrf_token']?>">
<input type="hidden" name="#command" value="/usr/local/emhttp/plugins/<?=$plugin?>/scripts/stop.sh">
<input type="submit" name="#stop" value="Stop MCP Server">
</form>

<form markdown="1" method="POST" action="/update.php" target="progressFrame">
<input type="hidden" name="csrf_token" value="<?=$var['csrf_token']?>">
<input type="hidden" name="#file" value="<?=$plugin?>/config.cfg">
<input type="hidden" name="#command" value="/usr/local/emhttp/plugins/<?=$plugin?>/scripts/apply.sh">

_(Unraid API URL)_:
: <input type="text" name="UNRAID_API_URL" value="<?=htmlspecialchars($cfg('UNRAID_API_URL'))?>">

<blockquote class="inline_help">
Leave blank to auto-detect from Unraid var.ini.
</blockquote>

_(API Key)_:
: <input type="password" name="UNRAID_API_KEY" value="<?=htmlspecialchars($cfg('UNRAID_API_KEY'))?>">
<blockquote class="inline_help">
Get your API key from Unraid webUI: My Server → API Keys → Create API Key
</blockquote>

_(Transport Mode)_:
: <select name="TRANSPORT">
  <option value="stdio" <?=($cfg('TRANSPORT') === 'stdio') ? 'selected' : ''?>>stdio</option>
  <option value="streamable-http" <?=($cfg('TRANSPORT') === 'streamable-http') ? 'selected' : ''?>>streamable-http</option>
  </select>

<blockquote class="inline_help">
stdio: MCP server runs as a local binary. streamable-http: MCP server runs as an HTTP server on the configured port.
</blockquote>

_(Bind Host)_:
: <input type="text" name="UNRAID_MCP_HOST" value="<?=htmlspecialchars($cfg('UNRAID_MCP_HOST', '127.0.0.1'))?>">

_(Port)_:
: <input type="number" name="UNRAID_MCP_PORT" value="<?=htmlspecialchars($cfg('UNRAID_MCP_PORT', '6970'))?>" min="1024" max="65535">

<div class="title"><span class="left"><i class="title fa fa-shield"></i>Security</span></div>

_(Read Only (Recommended))_:
: <select name="READ_ONLY">
  <option value="true" <?=($cfg('READ_ONLY') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('READ_ONLY') === 'false') ? 'selected' : ''?>>No</option>
  </select>
<blockquote class="inline_help">
Read-only mode blocks all state-changing operations (container/VM/array management, updates, file downloads, etc.)
</blockquote>

_(Allow Array Stop/Start)_:
: <select name="ALLOW_ARRAY_ACTIONS">
  <option value="true" <?=($cfg('ALLOW_ARRAY_ACTIONS') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_ARRAY_ACTIONS') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Array Stop)_:
: <select name="ALLOW_ARRAY_STOP">
  <option value="true" <?=($cfg('ALLOW_ARRAY_STOP') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_ARRAY_STOP') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Array Start)_:
: <select name="ALLOW_ARRAY_START">
  <option value="true" <?=($cfg('ALLOW_ARRAY_START') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_ARRAY_START') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Container Stop)_:
: <select name="ALLOW_CONTAINER_STOP">
  <option value="true" <?=($cfg('ALLOW_CONTAINER_STOP') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_CONTAINER_STOP') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Container Remove)_:
: <select name="ALLOW_CONTAINER_REMOVE">
  <option value="true" <?=($cfg('ALLOW_CONTAINER_REMOVE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_CONTAINER_REMOVE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Container Restart)_:
: <select name="ALLOW_CONTAINER_RESTART">
  <option value="true" <?=($cfg('ALLOW_CONTAINER_RESTART') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_CONTAINER_RESTART') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow VM Stop)_:
: <select name="ALLOW_VM_STOP">
  <option value="true" <?=($cfg('ALLOW_VM_STOP') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_VM_STOP') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow VM Force Stop)_:
: <select name="ALLOW_VM_FORCE_STOP">
  <option value="true" <?=($cfg('ALLOW_VM_FORCE_STOP') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_VM_FORCE_STOP') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow VM Reset)_:
: <select name="ALLOW_VM_RESET">
  <option value="true" <?=($cfg('ALLOW_VM_RESET') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_VM_RESET') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Plugin Install)_:
: <select name="ALLOW_PLUGIN_INSTALL">
  <option value="true" <?=($cfg('ALLOW_PLUGIN_INSTALL') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_PLUGIN_INSTALL') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Plugin Remove)_:
: <select name="ALLOW_PLUGIN_REMOVE">
  <option value="true" <?=($cfg('ALLOW_PLUGIN_REMOVE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_PLUGIN_REMOVE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Setting Updates)_:
: <select name="ALLOW_SETTING_UPDATES">
  <option value="true" <?=($cfg('ALLOW_SETTING_UPDATES') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_SETTING_UPDATES') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow SSH Update)_:
: <select name="ALLOW_SSH_UPDATE">
  <option value="true" <?=($cfg('ALLOW_SSH_UPDATE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_SSH_UPDATE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Time Update)_:
: <select name="ALLOW_TIME_UPDATE">
  <option value="true" <?=($cfg('ALLOW_TIME_UPDATE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_TIME_UPDATE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Notification Delete)_:
: <select name="ALLOW_NOTIFICATION_DELETE">
  <option value="true" <?=($cfg('ALLOW_NOTIFICATION_DELETE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_NOTIFICATION_DELETE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow API Key Create)_:
: <select name="ALLOW_API_KEY_CREATE">
  <option value="true" <?=($cfg('ALLOW_API_KEY_CREATE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_API_KEY_CREATE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow API Key Delete)_:
: <select name="ALLOW_API_KEY_DELETE">
  <option value="true" <?=($cfg('ALLOW_API_KEY_DELETE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_API_KEY_DELETE') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Flash Backup)_:
: <select name="ALLOW_FLASH_BACKUP">
  <option value="true" <?=($cfg('ALLOW_FLASH_BACKUP') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_FLASH_BACKUP') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Rsync Operations)_:
: <select name="ALLOW_RCLONE_OPERATIONS">
  <option value="true" <?=($cfg('ALLOW_RCLONE_OPERATIONS') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_RCLONE_OPERATIONS') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Connect Actions)_:
: <select name="ALLOW_CONNECT_ACTIONS">
  <option value="true" <?=($cfg('ALLOW_CONNECT_ACTIONS') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_CONNECT_ACTIONS') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Onboarding Actions)_:
: <select name="ALLOW_ONBOARDING_ACTIONS">
  <option value="true" <?=($cfg('ALLOW_ONBOARDING_ACTIONS') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_ONBOARDING_ACTIONS') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Docker Actions)_:
: <select name="ALLOW_DOCKER_ACTIONS">
  <option value="true" <?=($cfg('ALLOW_DOCKER_ACTIONS') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_DOCKER_ACTIONS') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow VM Actions)_:
: <select name="ALLOW_VM_ACTIONS">
  <option value="true" <?=($cfg('ALLOW_VM_ACTIONS') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_VM_ACTIONS') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Destructive (All))_:
: <select name="ALLOW_DESTRUCTIVE">
  <option value="true" <?=($cfg('ALLOW_DESTRUCTIVE') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('ALLOW_DESTRUCTIVE') === 'false') ? 'selected' : ''?>>No</option>
  </select>
<blockquote class="inline_help">
WARNING: Enabling this allows ALL destructive operations including array stop, disk remove, container delete, VM delete, plugin install/remove, system updates, and flash backup.
</blockquote>

<div class="title"><span class="left"><i class="title fa fa-comments"></i>MCP Access</span></div>

_(Bearer Token)_:
: <input type="password" name="UNRAID_MCP_BEARER_TOKEN" value="<?=htmlspecialchars($cfg('UNRAID_MCP_BEARER_TOKEN'))?>" autocomplete="off">
<blockquote class="inline_help">
Bearer token for authenticating MCP streamable-http connections. Required if HTTP auth is disabled.
</blockquote>

_(Disable HTTP Auth)_:
: <select name="UNRAID_MCP_DISABLE_HTTP_AUTH">
  <option value="false" <?=($cfg('UNRAID_MCP_DISABLE_HTTP_AUTH') === 'false') ? 'selected' : ''?>>Yes (recommended)</option>
  <option value="true" <?=($cfg('UNRAID_MCP_DISABLE_HTTP_AUTH') === 'true') ? 'selected' : ''?>>No (disable auth)</option>
  </select>

<div class="title"><span class="left"><i class="title fa fa-cog"></i>Logging</span></div>

_(Log Level)_:
: <select name="UNRAID_MCP_LOG_LEVEL">
  <option value="info" <?=($cfg('UNRAID_MCP_LOG_LEVEL') === 'info') ? 'selected' : ''?>>Info</option>
  <option value="debug" <?=($cfg('UNRAID_MCP_LOG_LEVEL') === 'debug') ? 'selected' : ''?>>Debug</option>
  <option value="warn" <?=($cfg('UNRAID_MCP_LOG_LEVEL') === 'warn') ? 'selected' : ''?>>Warn</option>
  <option value="error" <?=($cfg('UNRAID_MCP_LOG_LEVEL') === 'error') ? 'selected' : ''?>>Error</option>
  </select>

<div class="title"><span class="left"><i class="title fa fa-database"></i>TLS</span></div>

_(Verify SSL)_:
: <select name="UNRAID_VERIFY_SSL">
  <option value="true" <?=($cfg('UNRAID_VERIFY_SSL') === 'true') ? 'selected' : ''?>>Yes</option>
  <option value="false" <?=($cfg('UNRAID_VERIFY_SSL') === 'false') ? 'selected' : ''?>>No</option>
  </select>

_(Allow Insecure TLS)_:
: <select name="UNRAID_ALLOW_INSECURE_TLS">
  <option value="false" <?=($cfg('UNRAID_ALLOW_INSECURE_TLS') === 'false') ? 'selected' : ''?>>Yes</option>
  <option value="true" <?=($cfg('UNRAID_ALLOW_INSECURE_TLS') === 'true') ? 'selected' : ''?>>No</option>
  </select>
<blockquote class="inline_help">
Allow insecure TLS connections (self-signed certs, etc.).
</blockquote>

<input type="submit" name="#default" value="Default">
: <input type="submit" name="#apply" value="Apply"> <input type="button" value="Done" onclick="done()">
</form>